syntax = "proto2";

message Time {
  required uint32 hour   = 1;
  required uint32 minute = 2;
  required uint32 second = 3;
}

message SensorMessage {
  required float temperature = 1;
  required uint32 publisher_id = 2;
  required uint64 time = 3;
}
